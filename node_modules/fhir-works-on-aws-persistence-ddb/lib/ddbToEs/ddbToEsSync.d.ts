import { Client } from '@elastic/elasticsearch';
export declare class DdbToEsSync {
    private readonly ddbToEsHelper;
    private readonly getAliasFn;
    private readonly disableIndexAndAliasCreation;
    private readonly knownAliases;
    constructor({ esClient, getAliasFn, disableIndexAndAliasCreation, }?: {
        esClient?: Client;
        getAliasFn?: (ddbImage: any) => {
            alias: string;
            index: string;
        };
        disableIndexAndAliasCreation?: boolean;
    });
    handleDDBStreamEvent(event: any): Promise<void>;
}

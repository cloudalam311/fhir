import DynamoDB from 'aws-sdk/clients/dynamodb';
import { GenericResponse } from 'fhir-works-on-aws-interface';
export default class DynamoDbHelper {
    private dynamoDb;
    constructor(dynamoDb: DynamoDB);
    private getMostRecentResources;
    getMostRecentResource(resourceType: string, id: string, projectionExpression?: string, tenantId?: string): Promise<GenericResponse>;
    /**
     * @return The most recent resource that has not been deleted and has been committed to the database (i.e. The resource is not in a transitional state)
     */
    getMostRecentUserReadableResource(resourceType: string, id: string, tenantId?: string): Promise<GenericResponse>;
}

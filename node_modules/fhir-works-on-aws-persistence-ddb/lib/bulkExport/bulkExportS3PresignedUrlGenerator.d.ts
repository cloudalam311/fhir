import { BulkExportResultsUrlGenerator } from './bulkExportResultsUrlGenerator';
export declare class BulkExportS3PresignedUrlGenerator implements BulkExportResultsUrlGenerator {
    private readonly stsClient;
    constructor();
    getUrls({ s3Keys, exportBucket }: {
        exportBucket: string;
        s3Keys: string[];
    }): Promise<{
        requiresAccessToken: boolean;
        urls: string[];
    }>;
}

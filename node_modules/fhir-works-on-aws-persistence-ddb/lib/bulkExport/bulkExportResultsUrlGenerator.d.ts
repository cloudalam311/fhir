export interface BulkExportResultsUrlGenerator {
    getUrls(options: {
        exportBucket: string;
        s3Keys: string[];
    }): Promise<{
        requiresAccessToken: boolean;
        urls: string[];
    }>;
}
